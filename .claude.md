# 프로젝트: 유수정 포트폴리오 웹사이트

## 개요

Full-cycle 개발자 유수정의 개인 포트폴리오. 하반기 채용 서류 중 "URL 제출"용으로 사용.
Notion으로 만든 PDF 포트폴리오가 있었지만, PDF 추출 품질이 아쉬워서 웹사이트로 전환.

## 기술 스택

- Vue 3 (JavaScript, **TypeScript 미사용**)
- Vite
- Prettier
- 배포: GitHub Actions → GitHub Pages (정적 사이트, 백엔드/API 없음)

## 디자인 방향

- 무드: 미니멀 · 다크 · 깔끔함 (2026-08-05부터 다크 테마로 확정)
- 컬러 팔레트: Base `#0F172A`(배경), Point 1 `#38BDF8`(주요 accent — active 상태/인디케이터), Point 2 `#FBBF24`(보조 accent — 배지/KPI 강조)
- 레이아웃: 왼쪽 고정 사이드바 네비게이션 + 오른쪽 콘텐츠. 스크롤 기반 1페이지 구조로 최종 확정 (2026-08-05, 라우팅 방식 시도 후 되돌림) — 사이드바는 IntersectionObserver로 스크롤 위치를 감지해 active 표시, 클릭 시 해당 섹션으로 스무스 스크롤
- 네비게이션 항목(4개, 라벨은 "한국어 (영어)" 형식):
  1. `about` — 소개 (About Me)
  2. `skills` — 기술 스택 (Tech Skills). 하위에 "많이 해봤어요·해본 적 있어요·알아가고 있어요" 범례 텍스트 표시 (클릭 불가, 스크롤스파이 대상 아님)
  3. `projects` — 프로젝트 (Project)
  4. `activity` — 교내외 활동 (Activity). 하위 서브앵커 `activity-oncampus`(교내 활동) / `activity-offcampus`(교외 활동)를 가지며, 둘 다 스크롤스파이 대상 — 상위(04)와 하위 항목이 동시에 active 표시됨
  - Contact는 의도적으로 제외, 대신 사이드바 푸터에 이메일/GitHub/블로그를 "라벨 : 값" 텍스트로 노출 (이력서 다운로드는 기존대로 아이콘 버튼 유지)
- 히어로 섹션 컨셉: 텍스트 인사말보다 **성과 수치를 KPI 카드처럼 바로 노출** (예: "2년 서비스 단독 운영", "AWS 비용 80%↓", "API 응답속도 93%↓") — 채용담당자가 3초 안에 흥미를 느끼게 하는 것이 목표
- 컴포넌트 스타일: 얇은 border(0.5px) 위주, 그림자/그라데이션 최소화, 여백 넉넉하게

## 폴더 구조 (권장안)

```
src/
├── assets/
│   ├── images/
│   └── styles/
├── components/
│   ├── layout/       # Sidebar, Footer
│   ├── sections/      # HeroSection, AboutSection, SkillsSection, ProjectsSection, ActivitiesSection
│   └── ui/            # ProjectCard, SkillTag, StatCard 등 재사용 컴포넌트
├── data/               # skills.js, projects.js, activities.js, profile.js, aboutMe.js — 콘텐츠를 컴포넌트에서 분리
├── composables/
│   └── useScrollSpy.js
├── router/             # 라우팅 미사용, 1페이지 스크롤 구조라 필요성 낮음
├── stores/             # Pinia. counter.js는 예제 파일, 정리 필요. 공유 상태 생기면 사용
├── App.vue
└── main.js
```

## 콘텐츠 섹션 (Notion 포트폴리오 기준)

1. **About** — 소개 및 핵심 가치 5가지
2. **Skills** — 카테고리별(Language/Backend/Frontend/Database/DevOps/Security/Tools) 숙련도 3단계(많이 해봤어요/해본 적 있어요/알아가고 있어요)
3. **Projects** — 프로젝트별: 개요, Tech Stack, Key Responsibilities, 주요 성과, Problem Solving & Troubleshooting(문제상황→원인→해결) 구조
4. **Activities & Awards** — 수상 / 교외활동 / 교내활동 / 스터디 / 동아리
5. **Certifications** — TOPCIT, 한국사능력검정, OPIc, 정보처리기사

## 원본 데이터 소스

Notion 포트폴리오 페이지("유수정-포트폴리오")에 모든 프로젝트/활동/스킬 원본 텍스트가 정리되어 있음. `data/` 폴더 작성 시 이 내용을 참고.

## 작업 원칙

- 커밋 메시지: 영어, `type: summary` + bullet body 형식 (예: `feat: add hero section with KPI cards`)
- 파일 하나 완료될 때마다 커밋 제안
- 코드는 Claude가 직접 작성/구현 (2026-08-05부터 변경 — 이전에는 사용자가 직접 작성)
